apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexopus-api
  labels:
    app: nexopus-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nexopus-api
  template:
    metadata:
      labels:
        app: nexopus-api
    spec:
      containers:
      - name: api
        image: nexopus-api:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: nexopus-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: nexopus-secrets
              key: redis-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: nexopus-secrets
              key: secret-key
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: nexopus-api
spec:
  selector:
    app: nexopus-api
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
